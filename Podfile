platform :ios, '8.0'
use_frameworks!

def app_pods
    pod 'Fuzi', '~> 0.3.0'
	pod 'SwiftGifOrigin', '~> 1.5.3'
end
 
target 'GammeReader' do
    app_pods
end
 
target 'GammeReaderTests' do
    app_pods
end